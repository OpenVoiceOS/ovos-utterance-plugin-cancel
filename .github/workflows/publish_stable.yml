name: Publish Stable Release

on:
  workflow_dispatch:
  push:
    branches: [master, main]

permissions:
  contents: write   # required for version bump commit and release tag

jobs:
  publish_stable:
    if: github.actor != 'github-actions[bot]'
    uses: OpenVoiceOS/gh-automations/.github/workflows/publish-stable.yml@05c0bb438ca7d2bc485465bc233fd4e1997f23c5
    secrets:
      PYPI_TOKEN: ${{ secrets.PYPI_TOKEN }}
      MATRIX_TOKEN: ${{ secrets.MATRIX_TOKEN }}
    with:
      version_file: 'ovos_utterance_plugin_cancel/version.py'
      publish_pypi: true
      publish_release: true
      sync_dev: true
      notify_matrix: true
