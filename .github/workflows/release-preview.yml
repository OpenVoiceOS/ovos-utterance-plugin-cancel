name: Release Preview

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  release_preview:
    uses: OpenVoiceOS/gh-automations/.github/workflows/release-preview.yml@dev
    with:
      package_name: 'ovos_utterance_plugin_cancel'
      version_file: 'ovos_utterance_plugin_cancel/version.py'
